import {StyleSheet} from "react-native";

export const ShuffleButtonStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    backgroundColor: '#FDDE5F',
    borderRadius: 35,
    paddingHorizontal: 50,
    paddingVertical: 15,
  },
  text: {
    fontSize: 14,
    paddingHorizontal: 10,
    fontWeight: "500",
  },
});