import {StyleSheet} from "react-native";

export const InteractiveAreaStyles = StyleSheet.create({
  container: {
    flex: 5,
    justifyContent: "center",
    alignItems: "center",
    paddingTop: 35,
    paddingHorizontal: 30,
  },
})